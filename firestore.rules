rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read and write para pang prototype
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}